# Path to the DuckDB database file used to persist the query history buffer.
DUCKDB_STORAGE_PATH=./data/query_history.duckdb

# Number of days a history record is kept before it becomes eligible for purge.
MAX_HISTORY_RETENTION_DAYS=30
